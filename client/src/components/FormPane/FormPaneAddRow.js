import Row from 'react-bootstrap/Row';
import Col from 'react-bootstrap/Col';
import Button from 'react-bootstrap/Button';

export default function FormPaneAddRow() {
    return (
        <Row>
            <Col>
                <Button variant="primary">ROW</Button>
            </Col>
        </Row>
    )
}